# Web RAG 知识库问答系统 — 项目说明（简历版）

> 私有化可部署的检索增强生成（RAG）知识库问答系统。支持文档上传、向量索引构建与自然语言问答，返回带引用溯源的答案。面向企业内部知识管理场景。

---

## 项目简介

独立完成（/主导）一套全栈 RAG 知识库问答系统的设计与落地：用户可上传文档，系统自动完成解析、分块、向量化入库，并通过向量检索 + 大模型生成实现带引用的智能问答。支持本地化私有部署，保障数据不出域。

---

## 技术栈

- **后端**：FastAPI · SQLAlchemy 2.0(async) · asyncpg · PostgreSQL + pgvector · Celery + Redis · LlamaIndex
- **模型接入**：通义千问 `qwen` 系列 Embedding + LLM，经 OpenAI 兼容端点接入（OpenAILikeEmbedding）
- **安全与治理**：JWT 认证（python-jose + argon2）· RBAC 权限体系雏形 · Redis 令牌桶限流（Lua 原子脚本，全局 IP + 按用户双层级）
- **前端**：React 18 · TypeScript · Vite · Tailwind CSS · Zustand
- **工程化**：Docker Compose（6 服务编排）· Alembic 数据库迁移 · structlog 结构化日志 + Prometheus /metrics 指标（请求量 / 时延分布）

---

## 核心职责与亮点

- **RAG 问答核心链路端到端打通**：文档解析 → 智能分块 → 向量化入库 → 向量检索 → 流式 LLM 生成 → 引用溯源，构建完整闭环。
- **异步文档处理管道**：基于 Celery + Redis 实现"上传即异步处理"，以状态机管理文档生命周期（上传 / 排队 / 处理 / 完成 / 失败），不阻塞主流程。
- **流式问答与统一契约**：实现 SSE 流式响应，统一流式与非流式返回结构，保证前端稳定消费、历史可追溯。
- **多知识库权限体系**：落地 JWT 认证与 RBAC 雏形，支持多租户知识库隔离与成员角色管理。
- **生产可观测性**：结构化日志（X-Request-ID 全链路贯穿）+ Prometheus 指标端点（`/metrics`：请求总量、按时延桶分布的延迟直方图），支撑线上问题定位与容量评估。

---

## 代表性技术攻坚（面试可展开）

1. **向量检索 SQL 兼容**：修复 SQLAlchemy `text()` 中 `::` 类型转换与命名参数正则冲突导致的查询编译失败，改用 `CAST()` 统一参数绑定，彻底解决向量检索与入库的 SQL 注入兼容问题。
2. **Python 作用域陷阱**：定位并修复命名遮蔽引发的 `UnboundLocalError`（SQL 构造器 `text` 与局部变量同名），以导入别名规避，避免同类问题复现。
3. **检索置信度阈值调优**：针对所选中文 Embedding 模型在短文本上余弦相似度普遍偏低（0.4~0.6）的特性，基于真实向量距离分布实测，将拒答阈值由 0.7 下调至 0.35，解决"相关问题被误拒"。
4. **流式边界与历史一致性**：修复流式拒答分支返回类型不一致（异步生成器 vs 普通对象）导致的服务异常；修复历史对话前端加载与按时间升序的配对排序逻辑。
5. **本地化部署与运维**：解决 Celery 在 Windows 下的进程池兼容（solo 池）、任务注册、异步 ORM 关系反向同步清空外键等生产启动链路问题。

---

## 可选量化补充（按实际填写）

- 系统模块：auth / knowledge-base / document / chat 等，单体后端 + 前端分离架构。
- 处理性能：千字级文档可在秒级完成分块与向量化（基于接口耗时实测）。
- 部署形态：支持 Docker / 裸机私有化部署，数据全链路本地闭环。

---

## English Version

**Private, deployable RAG knowledge-base Q&A system.** Users upload documents; the system parses, chunks, and embeds them into a pgvector index, then answers natural-language questions with cited sources.

- **Stack**: FastAPI · SQLAlchemy 2.0(async) · PostgreSQL + pgvector · Celery + Redis · LlamaIndex · React 18/TS/Vite/Tailwind.
- **Highlights**: End-to-end RAG pipeline (parse → chunk → embed → retrieve → streamed LLM answer); async document processing via Celery/Redis with a status state machine; unified streaming/non-streaming response contract; JWT + RBAC multi-tenant isolation; observability (structlog structured logs + Prometheus `/metrics`).
- **Key problems solved**: SQLAlchemy `text()` `::` cast vs. bind-param conflict (fixed via `CAST()`); Python variable-shadowing `UnboundLocalError`; confidence-threshold tuning for short-text Chinese embeddings (0.7 → 0.35, backed by measured similarity distribution); streaming refusal-branch return-type mismatch; Celery Windows solo-pool and startup-link fixes.
