# Web RAG 技术开发深度分析报告

> 视角：资深全栈工程师 / 架构师，聚焦架构、代码质量、技术债、可扩展性、安全、性能、可维护性
> 审阅范围：backend 全部 20 个 Python 文件 + frontend 全部源码 + 部署配置（均有文件:行号佐证）

---

## 项目技术画像

- **后端**：FastAPI 0.104 + LlamaIndex 0.10 + SQLAlchemy 2.0(async) + asyncpg + pgvector/PG16
- **前端**：React 18 + TS 5 + Vite 5 + Tailwind 3
- **分层**：`routes → services → core(rag_engine/llm/embedding/parser) → db`
- **RAG 框架**：LlamaIndex（`SentenceSplitter` 切块 / `VectorStoreIndex` 索引 / `query_engine` 检索）

---

## 一、技术亮点与架构优势

**A1. 分层清晰，职责单一**
routes 仅做 HTTP 编解码与校验，services 编排业务，core 封装领域逻辑，db 管理持久化。FastAPI + Pydantic 的请求/响应模型定义规范。

**A2. 模型层抽象到位**
`core/llm.py`、`core/embedding.py` 用 provider 工厂模式屏蔽 OpenAI/Ollama/HF 差异，切换模型只改配置。这是后续支持私有化/多模型的关键基础。

**A3. 知识库级物理隔离**
每个 KB 一张 pgvector 表（`vector_store.py:15`），避免跨库向量污染，权限与计费边界天然。

**A4. 全链路 async + 流式**
SQLAlchemy async + asyncpg + SSE 流式（`chat.py` + `chat_service.chat_stream`），技术选型方向正确。

**A5. 配置集中化**
`config.py` 用 pydantic-settings + `@lru_cache`，环境驱动，规范。

> 评价：架构骨架是"合格的工程级 RAG 脚手架"，方向对、分层对。但**正确的选型被错误的实现细节拖累**——下面是关键技术债。

---

## 二、技术债务与风险诊断（按严重度排序）

### 【严重 P0】1. 同步阻塞事件循环 —— 全站性能与并发的头号杀手
LlamaIndex 的 `query_engine.query()` 与 `index_documents()` 是**同步阻塞调用**（含 embedding/LLM 的 HTTP 往返与 PDF 解析的 CPU 计算），却直接跑在 async 协程里：
- `rag_engine.py:89` `response = query_engine.query(question)`（sync）在 `async def query_stream` 链路中执行；
- `document_service.py:89-93` `DocumentParser.parse()` + `rag_engine.index_documents()` 在 `async def _process_document` 中**同步执行**。

后果：一次大文档入库（可达数十秒）会**冻结整个事件循环**，期间所有用户的请求（含健康检查）全部排队；流式问答期间 embedding 检索这一步同样阻塞。这违背了选 async 的初衷。
- **修复**：所有 LlamaIndex 同步调用用 `await asyncio.to_thread(...)` 包裹；或用 LlamaIndex 的 async API；文档处理整体移出 Web 进程（见 P0-2）。

### 【严重 P0】2. `asyncio.create_task` 火后即忘 —— 任务零可靠性
`document_service.py:66` `asyncio.create_task(self._process_document(...))`：
- 未持有 task 引用 → 可能被 GC 回收（"Task was destroyed but it is pending"）；
- 无持久化、无重试、无超时；**进程重启 = 任务丢失，文档永久卡在 `processing`**；
- 异常只写进 DB `error_message`，无告警。
- **修复**：引入任务队列（Arq+Redis / Dramatiq / Celery），持久化任务状态；启动时扫描 `processing` 超时文档自动重排。

### 【严重 P0】3. 删除不清理向量 —— 数据正确性 bug + 存储泄漏
- `document_service.delete_document`（:119-133）删文件 + 删行，但**不删 pgvector 中该文档的向量** → 检索仍返回已删除内容；
- `knowledge_base.py:99-110` 删 KB 走 FK 级联删 Document 行，但 `vectors_{uuid}` 表**不会被 DROP** → 孤儿表无限累积，存储与隐私双泄漏。
- **修复**：删文档时按 metadata（filename/doc_id）删向量；删 KB 时 `DROP TABLE vectors_xxx`。

### 【高 P1】4. 每请求重建 LLM/Embedding 实例 —— HF 模型每次重载
`llm.py` / `embedding.py` 的 `get_llm()` / `get_embedding_model()` **无缓存**，每次 `RAGEngine.__init__` 都新建。若用 `HuggingFaceEmbedding`，每次请求**从磁盘重载模型到内存**（秒级延迟 + 数百 MB 内存抖动）。`LlamaSettings` 是全局可变对象，却被每请求重写（`rag_engine.py:28-31`），属状态污染反模式。
- **修复**：`get_llm` / `get_embedding_model` 加 `@lru_cache` 或放 `app.state` 单例；`LlamaSettings` 启动时配置一次。

### 【高 P1】5. 向量库连接参数靠手撕字符串 —— 密码含特殊字符即崩
`vector_store.py:30-54` 用 `split("@") / split(":")` 解析 DB URL。密码含 `@`、`:`、`/`（云数据库常见）→ 解析错乱、连接失败或串库。
- **修复**：用 `sqlalchemy.engine.URL.create()` 或 `urllib.parse.urlparse` 解析；最好直接复用 `database.py` 的 engine URL。

### 【高 P1】6. embed_dim 硬编码 1536 —— 换模型即维度错配
`vector_store.py:24` 写死 1536。切 `bge-large-zh-v1.5`(1024) / `bge-m3`(1024) / `text-embedding-3-large`(3072) → 插入报错或静默错配。
- **修复**：`embed_dim` 提升为 config，与 embedding 模型绑定；启动时校验已有表维度。

### 【高 P1】7. 安全裸奔
- `main.py:48-54` CORS `allow_origins=["*"]` + `allow_credentials=True`（组合本身违规）；
- **全仓无认证/鉴权**，任何匿名请求可建/删 KB、上传、查询；
- `backend/.env` 出现在仓库中（glob 可见）→ **疑似密钥入库**，需核查 git 历史；
- `document_parser.parse_url`（:148）`requests.get(url)` 无 SSRF 防护（目前无路由暴露，属定时炸弹）；
- 上传仅校验扩展名 + 大小，无 magic-byte/内容校验、无病毒扫描、无限流。
- **修复**：收紧 CORS 白名单；加 API Key/JWT + RBAC；`.env` 移出仓库并加 `.gitignore`；SSRF 黑名单；上传内容校验 + 限流。

### 【高 P1】8. 无数据库迁移
`init_db()` 用 `Base.metadata.create_all`（`database.py:44-47`），`alembic` 在依赖里却**无迁移目录**。生产改表只能手动 drop/recreate，数据必丢。
- **修复**：初始化 Alembic，首个 baseline 后所有 schema 变更走迁移。

### 【中 P2】9. 前端：Markdown 渲染缺失 + 流式状态可变
- `react-markdown` 已装但 `MessageBubble.tsx:21` 用 `whitespace-pre-wrap` 纯文本——LLM 的表格/代码/列表全糊成文本；
- `ChatPage.tsx:80-87` 流式更新直接 `last.content += chunk` **原地变更 state 对象**（反模式），靠父组件重渲染"碰巧"生效；
- 无 `AbortController`，切页/重发时旧流不取消；无错误边界、无 toast、无暗色模式、无移动端适配（侧栏固定 `w-64`）。
- **修复**：接入 `ReactMarkdown` + 代码高亮；流式用不可变更新；加 abort + 错误边界 + 暗色 + 响应式。

### 【中 P2】10. 检索质量工程缺陷
- `rag_engine.py:84` `response_mode="compact"`、`temperature=0.7`（`llm.py:27`）——事实型问答温度过高，助长幻觉，应为 0.0–0.2；
- 仅 top-k 向量召回，无 rerank、无 BM25 混合、无 query 改写、无多跳、无拒答/置信度门；
- `source.text` 截断 200 字仅用于展示，无引用-答案句级映射。
- **修复**：温度调低；接 bge-reranker + 混合检索；加置信度门与拒答。

### 【中 P2】11. 文档解析丢结构 + 编码脆弱
- `_parse_word`（:88-96）只读段落，**忽略 `doc.tables`**，表格丢失；`_parse_csv` 转纯文本丢失结构；
- 无 OCR，扫描件 PDF 直接报"无文本"失败；`_parse_pdf` 用 `except Exception` 吞异常再 fallback；
- 文本/HTML/MD 统一 `encoding="utf-8"` 硬编码，GBK 文件即崩。
- **修复**：表格保形（Unstructured/MinerU）；扫描件 OCR；编码自动探测（chardet）。

### 【中 P2】12. 可观测性为零
只有 `print()`（`main.py:29-31`），无结构化日志、无 metrics、无 trace、无 request-id；`/api/health` 返回静态，不探活 DB/LLM。
- **修复**：structlog + OpenTelemetry；健康检查探 DB ping + LLM 可达。

### 【中 P2】13. 工程化与部署
- 无任何测试（pytest / 前端均无）；无 ESLint/Prettier 配置；
- `start.bat` 与 `main.py:__main__` 带 `--reload`（仅 dev）；`docker-compose.yml` 只含 PG，无应用镜像；无 Dockerfile；无 CI/CD；`index.html` 引用不存在的 `/vite.svg`。
- **修复**：加 pytest + httpx 接口测试、Vitest；Dockerfile + compose 全量编排；CI（lint/test/build）。

### 【低 P3】14. 依赖与 API 旧式
- `llama-index 0.10.68` 偏旧；`starlette==0.27.0` 与 fastapi 显式同锁（脆弱）；
- pydantic v2 仍用 `class Config`（已弃用，应 `model_config = SettingsConfigDict(...)`）；
- `datetime.utcnow`（Python 3.12+ 弃用，应 `datetime.now(timezone.utc)`）。

---

## 三、技术架构升级方向（创新点）

**T1. 把"同步 RAG"重构为"队列驱动的异步管道"**
文档处理拆为独立 worker（Arq/Celery），Web 进程只入队；查询路径用 `to_thread` 卸载阻塞。这是把"demo"变"可生产"的分水岭，直接解决 P0-1 / P0-2。

**T2. 引入检索质量评估闭环（eval harness）**
建垂直评测集 + 跑 faithfulness / context_recall / answer_relevancy（RAGAS）。让"调 chunk_size / top_k / 温度"从玄学变数据驱动。这是 RAG 工程化的核心缺失。

**T3. 检索增强栈：rerank + 混合检索 + 置信度门**
向量召回 → BM25 混合 → bge-reranker 重排 → 置信度门拒答。性价比最高的精度与可信度提升，且可灰度。

**T4. 索引/查询层抽象 + 增量索引**
当前每次 `index_documents` 全量建索引，无增量、无去重、无文档级删除支持。抽象出 `IndexManager`（增/删/重建），配合 P0-3 的向量清理。

---

## 四、技术改进路线图（90 天）

### Sprint 1（Day 1–30）：堵住致命漏洞，可上 demo
- [P0] LlamaIndex 同步调用全部 `to_thread` 包裹（消除事件循环阻塞）
- [P0] 引入 Arq + Redis 任务队列替代 `create_task`；启动自愈卡死任务
- [P0] 删文档 / 删 KB 清理向量与 DROP 表（修正确性 bug）
- [P1] `get_llm` / `get_embedding_model` 单例化；`LlamaSettings` 启动时配一次
- [P1] `embed_dim` 配置化；URL 解析换 `urlparse`
- [P1] 温度调 0.1；接 bge-reranker + BM25 混合检索
- [P1] 前端接 ReactMarkdown + 流式不可变更新 + abort
- **验收**：50MB 文档上传不阻塞其它请求；删文档后检索不再命中；并发 10 路问答 P95 首字 < 2s

### Sprint 2（Day 31–60）：工程化与质量基线
- [P1] Alembic 迁移 baseline；收紧 CORS；加 API Key + RBAC；`.env` 出库
- [P1] 结构化日志 + request-id + 健康/就绪探针
- [P1] 文档解析表格保形 + 编码探测 + 扫描件 OCR
- [P2] RAGAS 评测集（50–100 条）+ CI 跑回归
- [P2] pytest 接口测试 + Vitest 前端测试 + ESLint
- **验收**：faithfulness ≥ 85%；核心接口测试覆盖 ≥ 70%；schema 变更走迁移

### Sprint 3（Day 61–90）：生产化与可扩展
- [P2] Dockerfile + 全量 compose 编排 + nginx + 多 worker（去 reload）
- [P2] 限流 + token 预算 + 上传内容校验
- [P2] 置信度门 + 拒答 + 引用-答案句级映射
- [T4] IndexManager 增量索引 / 去重
- [P3] 依赖升级（llama-index / pydantic v2 写法 / datetime）
- **验收**：可一键私有化部署；限流生效；增量索引支持文档级更新

> **技术北极星**：Sprint 1 结束时，系统在并发与故障下不崩、不脏数据——这是从"能跑"到"能上线"的最低门槛。

---

## 待补充的技术信息

1. 目标并发量与数据规模（KB 数、单库文档数、平均文档大小）——决定队列 / 连接池 / 向量索引参数。
2. 部署形态：单机 / 容器 / K8s？是否必须私有化（决定是否引入 Redis / 消息中间件）。
3. 模型策略：是否锁定本地模型（Ollama / HF）——影响 embed_dim、显存、并发模型。
4. 是否已有测试 / CI 基线、代码仓库与分支策略。
5. 现有线上故障史（卡死 / 串库 / OOM）——帮助校准优先级。

---

*报告基于对 backend 20 个 Python 文件 + frontend 全部源码 + docker-compose/init.sql/start.bat 的逐行审阅。所有结论均有 `文件:行号` 级佐证。*
