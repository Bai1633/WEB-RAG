# Web RAG 项目重构提示词（高并发工程化版）

> 用法：把本文件整段作为提示词喂给 AI 编程助手（或交给开发团队）。已按你确认的技术栈写死，AI 不得擅自更换。
> 已确认技术栈：Python 3.12 + FastAPI（最新）｜Redis + Celery｜PostgreSQL 16 + pgvector｜LlamaIndex（最新）｜React 18 + Vite + Tailwind

---

## 0. 角色设定

你是一位资深全栈架构师 + 高级后端/前端工程师，精通：Python 异步高并发、RAG 检索系统、PostgreSQL/pgvector、Celery 分布式任务、Redis 缓存与限流、React 工程化、Docker/CI/CD。你的任务是把一个"能跑的 RAG Demo"**从骨架开始重构**成"可生产、支持高并发、工程化"的知识库问答系统。代码必须生产级：类型完整、可测试、可观测、可水平扩展。

---

## 1. 项目背景与现状（必读）

现有项目 `web_rag` 是一个 AI 知识库问答系统，核心闭环：
**建知识库 → 上传文档（PDF/Word/Markdown/HTML/TXT/CSV）→ 解析切块入 pgvector → SSE 流式问答（带引用来源）**。

当前是 Demo 级，存在以下必须修复的致命问题（重构要逐条解决）：
1. LlamaIndex 的同步调用（`query`/`index_documents`）直接跑在 async 协程里，**阻塞事件循环**；
2. 文档处理用 `asyncio.create_task` 火后即忘，无队列、无重试，**进程重启任务丢失、文档永久卡 `processing`**；
3. 删文档/删知识库**不清理 pgvector 向量与向量表**（正确性 bug + 存储泄漏）；
4. LLM/Embedding 客户端**每请求重建**，HF 模型每请求从磁盘重载；
5. `embed_dim` 硬编码 1536，换模型即维度错配；
6. 向量库连接参数靠手撕字符串解析，密码含特殊字符即崩；
7. **全仓无认证/鉴权**，CORS 为 `*`；
8. 无 Alembic 迁移（用 `create_all`）、无测试、无日志/可观测；
9. 前端装了 `react-markdown` 却没用，回答以纯文本呈现；流式更新原地变更 state；无中断/暗色/响应式。

**重构目标**：保留全部业务功能，把以上问题全部解决，提升到生产级 + 高并发。

---

## 2. 目标技术栈（已确认，禁止更换）

| 层 | 技术选型 |
|---|---|
| 语言/框架 | Python 3.12 + FastAPI（最新稳定版） |
| ASGI/进程 | Gunicorn + UvicornWorker 多进程 + Nginx 反向代理 |
| 任务队列 | Celery + Redis（broker） |
| 缓存/限流/分布式锁 | Redis |
| 数据库 | PostgreSQL 16 + pgvector |
| ORM/迁移 | SQLAlchemy 2.0（async）+ asyncpg + Alembic |
| RAG 引擎 | LlamaIndex（最新 0.12.x） |
| LLM/Embedding | OpenAI SDK（async）/ Ollama / sentence-transformers（本地），provider 抽象 + 单例缓存 |
| 认证授权 | JWT（访问+刷新令牌）+ Argon2 密码哈希 + RBAC |
| 校验/配置 | Pydantic v2 + pydantic-settings |
| 可观测 | structlog + OpenTelemetry + Prometheus + Sentry |
| 测试 | pytest + pytest-asyncio + httpx + RAGAS |
| 前端 | React 18 + TypeScript + Vite + Tailwind CSS + Zustand + react-markdown |
| 基础设施 | Docker 多阶段构建 + docker-compose + GitHub Actions |

---

## 3. 架构与目录结构（从骨架开始）

采用清晰分层：`api(routes) → services → core(rag/index/llm/embedding/parser) → db(models/repositories) → workers(celery)`，依赖只允许向下。请先输出**完整目录树**，参考骨架：

```
web_rag/
├── backend/
│   ├── alembic/                  # 迁移
│   ├── app/
│   │   ├── main.py               # 应用工厂、中间件、路由注册、生命周期
│   │   ├── config.py             # pydantic-settings，分环境
│   │   ├── api/
│   │   │   ├── deps.py           # 依赖注入（DB/当前用户/权限）
│   │   │   └── routes/           # auth, users, knowledge_bases, documents, chat, health
│   │   ├── core/
│   │   │   ├── security.py       # JWT/Argon2/RBAC
│   │   │   ├── llm.py            # LLM provider 抽象 + 单例
│   │   │   ├── embedding.py      # Embedding provider 抽象 + 单例
│   │   │   ├── document_parser.py# 解析（表格保形/OCR/编码探测）
│   │   │   ├── chunking.py       # 切块策略
│   │   │   ├── rag_engine.py     # 检索/重排/生成/引用
│   │   │   └── index_manager.py  # 向量增删/重建/维度校验
│   │   ├── db/
│   │   │   ├── base.py  database.py  models.py  repositories/
│   │   ├── services/             # auth, knowledge_base, document, chat
│   │   ├── workers/
│   │   │   ├── celery_app.py     # Celery 实例/配置
│   │   │   └── tasks.py          # 文档处理等任务（幂等+重试）
│   │   ├── schemas/              # Pydantic 请求/响应模型
│   │   └── utils/                # logging, metrics, exceptions, rate_limit
│   ├── tests/                    # unit + integration + eval
│   ├── alembic.ini  pyproject.toml  Dockerfile  .env.example
├── frontend/
│   ├── src/
│   │   ├── api/  stores/  pages/  components/  hooks/  lib/  types/
│   │   ├── App.tsx  main.tsx  index.css
│   ├── Dockerfile  nginx.conf  package.json
├── deploy/
│   ├── docker-compose.yml        # app + worker + redis + pg + nginx
│   ├── nginx/  .github/workflows/ci.yml
└── README.md
```

---

## 4. 数据模型（给出 DDL + Alembic 迁移）

至少包含以下表，字段/索引/外键/级联规则补全，并生成首个 Alembic 迁移：
- **users**：id, email(唯一), hashed_password, is_active, created_at
- **knowledge_bases**：id, owner_id→users, name, description, embed_model, embed_dim, vector_table(每库独立 `vectors_<uuid>`), created_at
- **kb_members**（RBAC）：kb_id, user_id, role(owner/editor/viewer)
- **documents**：id, kb_id, filename, file_path, file_size, mime_type, status(uploaded/queued/processing/completed/failed), progress, error, chunk_count, task_id, created_at, updated_at
- **chat_histories**：id, kb_id, user_id, question, answer, sources(JSONB), tokens, latency_ms, created_at
- **向量表**（每 KB 一张 pgvector 表）：chunk_id, doc_id, kb_id, text, metadata(JSONB), embedding vector(embed_dim)
- 索引：外键、状态、 created_at、向量 HNSW 索引（调 m/ef_construction）

---

## 5. 核心业务流程（给出实现）

1. **认证**：注册/登录/刷新/登出；JWT 访问+刷新令牌；`deps.py` 提供 `current_user` 与 KB 级 RBAC 校验依赖。
2. **知识库 CRUD**：创建时生成 `vectors_<uuid>` 表并记录 `embed_dim`；删除时 **DROP 向量表** + 级联清理。
3. **文档上传管道（全异步）**：
   - 上传接口：流式落盘（不全量读内存）→ 写 documents(status=uploaded) → **入队 Celery** → 立即返回 task_id；
   - Celery 任务：解析(表格保形/OCR/编码探测) → 切块 → embedding → 入 pgvector，更新 status/progress/chunk_count；
   - **幂等**（重复入队不重复入库）+ **指数退避重试** + 失败写 error + 死信；启动时扫描 `processing` 超时文档自动重排（卡死自愈）。
4. **RAG 问答（SSE 流式）**：
   - 检索：向量召回 + BM25 混合 → bge-reranker 重排；
   - **所有 LlamaIndex/解析等同步阻塞调用用 `asyncio.to_thread` 卸载**，绝不在事件循环里直接跑；
   - 生成：temperature=0.1，流式输出；**置信度门**：检索分数低于阈值 → 拒答"知识库未找到依据"；
   - 引用：返回来源 + 相关度 + 句级映射；落库 chat_histories（独立短会话，不占请求连接）。
5. **删除一致性**：删文档同步删除其向量（按 doc_id metadata 过滤）；删 KB 同步 DROP 向量表。

---

## 6. 高并发设计（硬性要求）

- 全链路 async；DB 用 asyncpg 连接池（调 pool_size/max_overflow）；
- LLM/Embedding 客户端**单例缓存**（`@lru_cache` 或 `app.state`），禁止每请求重建；
- Redis：检索结果缓存、会话缓存、**令牌桶限流**（按用户/IP）、分布式锁；
- Celery worker 多实例水平扩展，队列深度可监控；
- 背压：上传大小/并发限制、请求超时、队列长度告警；
- 目标：**≥200 并发问答，P95 首字延迟 < 2s；50MB 文档上传不阻塞其它请求**。

## 7. 可靠性与一致性

- 任务幂等 + 重试 + 死信 + 崩溃自愈；
- `embed_dim` 提升为配置并与 embedding 模型绑定，启动时校验已有向量表维度；
- DB 连接串用 `sqlalchemy.engine.URL` / `urllib.parse.urlparse` 解析，禁止手撕字符串；
- 多步写入保证事务一致，失败可回滚。

## 8. 安全

- 全接口默认需认证（除健康检查/登录注册）；KB 级 RBAC；
- CORS 白名单（禁止 `*` + credentials 同开）；
- 上传：magic-byte 校验 + 类型/大小白名单 + 限流；
- URL 解析功能加 **SSRF 防护**（内网 IP 黑名单）；
- 密钥走环境变量/secret 管理，`.env` 不入库（提供 `.env.example`）；密码 Argon2。

## 9. RAG 质量

- 切块：语义切块 + 重叠；表格保形（不拆表）；扫描件 OCR；文本编码自动探测（chardet）；
- 检索：向量 + BM25 混合 + bge-reranker；温度 0.1；置信度门 + 拒答；引用句级映射；
- 评测：接入 **RAGAS**（faithfulness / context_recall / answer_relevancy），提供 50–100 条评测集与跑分脚本。

## 10. 可观测性

- structlog 结构化日志 + request-id 贯穿；
- Prometheus metrics：QPS、延迟分位、队列深度、LLM token 用量、文档处理成功率；
- OpenTelemetry tracing；Sentry 异常上报；
- `/health`（存活）与 `/ready`（探 DB/Redis/LLM 可达）探针。

## 11. 前端要求

- 页面：登录/注册、知识库管理、文档管理（上传/进度/删除/重试）、聊天；
- 聊天：**react-markdown** 渲染 + 代码高亮；流式输出用**不可变更新** + `AbortController` 可中断；引用来源展示；思考中骨架屏；
- Zustand 全局状态；错误边界 + toast；**暗色/浅色/跟随系统主题切换**；响应式 + 移动端适配。

## 12. 部署 / DevOps

- 后端 Dockerfile（多阶段）+ 前端 Dockerfile（构建 + Nginx 托管）；
- docker-compose：`app + worker + redis + pg(+pgvector) + nginx`，含健康检查与依赖顺序；
- Nginx：反代 + TLS + gzip + 客户端限流；Gunicorn + UvicornWorker（去掉 `--reload`）；
- 环境分 dev/staging/prod（pydantic-settings 分组）；
- GitHub Actions：lint(ruff/mypy) + test + build + 镜像扫描。

## 13. 测试

- 单元测试：parser/chunking/embedding 抽象/权限；
- 集成测试：httpx 打主要 API（认证/KB/文档/问答）；
- RAGAS 评测脚本纳入 CI；
- 核心接口覆盖率 **≥ 70%**。

## 14. 开发规范

- 类型注解全覆盖 + mypy 严格；ruff + black；pre-commit；
- Conventional Commits；feature 分支 + PR 评审；
- 每个公共模块/函数写 docstring。

## 15. 非功能验收标准（必须全部满足）

- 并发：≥200 并发问答，P95 首字延迟 < 2s；50MB 上传不阻塞其它请求；
- 可靠：文档处理成功率 ≥ 99%；worker 崩溃任务可恢复；删文档后检索不再命中；
- 质量：RAGAS faithfulness ≥ 0.85；
- 安全：未认证请求全部被 401/403 拦截；通过基础渗透检查；
- 工程：`docker-compose up` 一键起全栈；CI 全绿。

## 16. 交付与里程碑

- **M1**：项目骨架 + 认证/RBAC + 知识库 CRUD + 异步文档上传管道（Celery）+ Alembic 迁移；
- **M2**：RAG 问答（SSE 流式 + 混合检索 + rerank + 拒答 + 引用）+ 前端三页 + 主题切换；
- **M3**：可观测 + 测试 + Docker/CI + 性能压测报告。
- 每个里程碑交付：可运行产物 + README + 本里程碑验收清单。

## 17. 约束

- 不得更换第 2 节已确认的技术栈；
- 保留现有全部业务功能；提供**旧数据迁移指南**（旧 documents/向量如何迁入新 schema）；
- 任何LlamaIndex/解析等阻塞调用不得出现在事件循环中。

---

## 18. 输出要求（重要）

**先不要直接写全部代码**。第一步只输出：
1. 完整目录树；2. 架构图（组件+数据流）；3. 数据模型 DDL + 首个 Alembic 迁移；4. 各里程碑任务拆解。

待我确认骨架无误后，再按 M1 → M2 → M3 分模块输出可运行代码，每模块附测试与说明。
