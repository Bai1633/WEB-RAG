# Web RAG 项目运行指南（v2.1）

> 目标：在本机把「前端 + 后端 + 数据库 + 任务队列」全部跑起来，完成 **注册 → 建库 → 传文档 → 问答** 的完整闭环。
>
> 本版本已对齐当前代码：`docker compose` 一键起 PostgreSQL + Redis、Celery worker **与 beat**、前端 axios 客户端（`src/api/client.ts`）、默认模型走阿里云百炼 DashScope。

---

## 0. 环境前置

| 依赖 | 版本要求 | 说明 |
| --- | --- | --- |
| Docker Desktop | 最新 | 起 PostgreSQL(pgvector) 与 Redis |
| Python | 3.11+（推荐 3.11 / 3.12） | llama-index 0.12 需要较新版本 |
| Node.js | 18+ | 前端 Vite |
| LLM / Embedding API Key | 默认阿里云百炼 DashScope | 问答与向量化必需；不填接口会报错 |

**涉及端口**：PostgreSQL `5432`、Redis `6379`、后端 `8000`、前端 `3000`。

---

## 1. 启动基础设施：PostgreSQL + Redis

根目录 `docker-compose.yml` **已同时包含 postgres 与 redis**，直接起：

```bash
cd D:\my_project\web_rag
docker compose up -d
```

该 compose 会拉起：
- `pgvector/pgvector:pg16`（容器名 `web_rag_postgres`，库 `web_rag`，挂载 `init.sql` 启用 `vector` 扩展）
- `redis:7-alpine`（容器名 `web_rag_redis`）

验证：

```bash
docker ps                                       # web_rag_postgres 与 web_rag_redis 均为 Up
docker exec -it web_rag_redis redis-cli ping    # 返回 PONG
```

> 只需单独补一个 Redis 时才用：`docker run -d --name web_rag_redis -p 6379:6379 redis:7`（正常情况下不需要，compose 已含）。

---

## 2. 后端配置（.env）

```bash
cd backend
copy .env.example .env      # Linux / macOS 用 cp .env.example .env
```

编辑 `.env` 的**必改项**（默认走阿里云百炼 DashScope 的 OpenAI 兼容端点）：

```ini
# 1) 对话模型
LLM_API_KEY=sk-你的key
LLM_MODEL_NAME=qwen3.7-flash
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 2) 向量模型（维度必须与模型一致！）
EMBEDDING_API_KEY=sk-你的key
EMBEDDING_MODEL_NAME=qwen3.7-text-embedding
EMBEDDING_DIM=1024
```

> ⚠️ **Windows 必改**：`.env.example` 中 `DB_HOST`、`REDIS_URL`、`CELERY_BROKER_URL`、`CELERY_RESULT_BACKEND` 默认写的是 `localhost`。Windows 会把 `localhost` 解析成 IPv6 `::1`，而 Docker Desktop 的端口转发对 `::1` 不生效，会导致每个请求干等约 5s 甚至超时。**请全部改成 `127.0.0.1`**（例如 `DB_HOST=127.0.0.1`、`REDIS_URL=redis://127.0.0.1:6379/0`）。
> 例外：`CORS_ORIGINS` 保持 `localhost`（那是浏览器 origin，不是容器连接串）。

**其它模型方案（可选）**：

```ini
# OpenAI 官方
LLM_BASE_URL=https://api.openai.com/v1
EMBEDDING_MODEL_NAME=text-embedding-ada-002
EMBEDDING_DIM=1536

# 本地 Ollama（免 API Key）
LLM_PROVIDER=ollama
LLM_BASE_URL=http://localhost:11434

# 本地 HuggingFace embedding
EMBEDDING_PROVIDER=huggingface
EMBEDDING_MODEL_NAME=BAAI/bge-large-zh-v1.5
EMBEDDING_DIM=1024
```

> `EMBEDDING_PROVIDER=mock` 提供确定性哈希向量，仅供 CI / 本地冒烟，无语义意义，勿用于真实问答。

---

## 3. 安装后端依赖

```bash
cd backend
python -m venv venv
venv\Scripts\activate                       # macOS / Linux: source venv/bin/activate
pip install -r requirements.txt
```

> llama-index / torch 体积较大，首次安装需几分钟；若中断，重跑一次 `pip install -r requirements.txt` 即可。

---

## 4. 建表

**推荐（规范做法）**：

```bash
cd backend
alembic upgrade head
```

迁移 `0001` / `0002` 均已做幂等处理，可安全重复执行。

**或**：直接启动后端，`main.py` 的 lifespan 会调用 `init_db()` 自动建表（注意：只补「表」，不补「列」，旧库缺列请用迁移）。

验证：`GET /api/ready` 出现 `database: ok`；或
```bash
docker exec -it web_rag_postgres psql -U postgres -d web_rag -c "\dt"
```

---

## 5. 启动后端 API

```bash
cd backend
venv\Scripts\activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

验证：
- `GET http://localhost:8000/api/health` → `{"status":"healthy"}`
- `GET http://localhost:8000/api/ready` → `{"status":"ready","checks":{"database":"ok","redis":"ok"}}`
- Swagger 文档：`http://localhost:8000/docs`

> **冷启动预热**：启动后 lifespan 会在后台预热 embedding（首次约 22s）与 reranker（首次约 52s，若启用）。日志出现 `embedding_warmed_up` / `reranker_warmed_up` 才算就绪；在此之前首次提问会明显偏慢，属正常现象，不要误判为卡死。

---

## 6. 启动 Celery（异步文档处理）

### 6.1 worker（必需）

```bash
cd backend
venv\Scripts\activate
celery -A app.workers.celery_app.celery_app worker --loglevel=info
```

> **不起 worker，上传的文档会一直停在 `processing`**，问答也检索不到内容。

### 6.2 beat（建议启动，跑定时清理）

```bash
cd backend
venv\Scripts\activate
celery -A app.workers.celery_app.celery_app beat --loglevel=info
```

定时任务（见 `app/workers/celery_app.py` 的 `beat_schedule`）：
- `cleanup_stuck_documents`：每 **10 分钟**，捞回卡在 `processing` / `queued` 的超时文档并重派。
- `cleanup_expired_refresh_tokens`：每 **24 小时**，清理过期 refresh token。

> Windows 上 `celery_app.py` 已自动把 worker pool 设为 `solo`，无需手动加 `--pool` 参数。

---

## 7. 启动前端

```bash
cd frontend
npm install
npm run dev
```

浏览器打开 `http://localhost:3000`。

**接口地址说明**：
- 前端默认**直连**后端 `http://localhost:8000`（见 `src/api/client.ts` 的 `VITE_API_BASE_URL || 'http://localhost:8000'`）。后端换端口时，改该文件默认值或设环境变量 `VITE_API_BASE_URL`。
- `vite.config.ts` 也配了 `/api` → `localhost:8000` 代理，但当前客户端用的是绝对地址，实际走直连。
- 认证 token 由 axios 请求拦截器自动注入；收到 401 会自动用 refresh token 静默续期（同样在 `src/api/client.ts`）。

---

## 8. 完整验证闭环

1. 打开 `http://localhost:3000/register` 注册账号
2. 跳 `/login` 登录
3. 进入 `/manage` 创建知识库
4. 上传一个 PDF / Markdown 文件
5. **观察 Celery worker 终端日志**，文档状态 `processing` → `completed`
6. 进入 `/kb/:kbId/chat`，提问，应看到流式回答 + 引用来源（sources）

全部跑通即代表链路 OK。

---

## 9. 一键启动脚本（可选）

`start.bat`（内部调用 `start.ps1`）会依次启动：PostgreSQL → Redis → 后端 API → Celery worker → 前端，幂等跳过已运行的服务；`.env` 不存在时会从示例复制并提示编辑。

> 注意：脚本**不启动 Celery beat**；`.env` 里连接串仍请自行确认是 `127.0.0.1`。

---

## 10. 常见问题（含历史踩坑）

1. **Windows 连不上 PostgreSQL / Redis**：连接串用 `127.0.0.1`，别用 `localhost`（IPv6 `::1` 问题，见第 2 节）。
2. **文档一直 `processing`**：没起 Celery worker（第 6.1 节）。
3. **首次提问很慢 / 像卡住**：embedding 与 reranker 冷加载（22s / 52s）。重排权重需放在 `backend/models/bge-reranker-v2-m3`（约 2.27GB，已被 `.gitignore` 忽略，需单独下载）；未就绪时可临时 `RERANK_ENABLED=false` 关闭重排。
4. **入库报向量维度错误**：`EMBEDDING_DIM` 与所选模型输出不一致（`qwen3.7-text-embedding` = 1024）。
5. **端口 8000 被占用 / 请求没走到新代码**：`netstat -ano | findstr ":8000"` 确认监听进程；Windows 上可能出现"静默双绑"，先杀掉旧进程再启。
6. **换了后端端口后前端接口全失败**：改 `frontend/src/api/client.ts` 的默认 baseURL，或设置 `VITE_API_BASE_URL`。

---

## 快速命令清单（复制即用）

```bash
# 1. 基础设施（PostgreSQL + Redis）
docker compose up -d

# 2. 后端
cd backend
copy .env.example .env        # 记得改 127.0.0.1 并填 Key
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --port 8000 --reload

# 3. Celery worker（新终端，必需）
cd backend && venv\Scripts\activate
celery -A app.workers.celery_app.celery_app worker --loglevel=info

# 4. Celery beat（新终端，建议）
cd backend && venv\Scripts\activate
celery -A app.workers.celery_app.celery_app beat --loglevel=info

# 5. 前端（新终端）
cd frontend
npm install
npm run dev
```
