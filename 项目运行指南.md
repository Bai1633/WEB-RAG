# Web RAG 项目运行指南（v2.0）

> 目标：在本机把前后端 + 数据库 + 任务队列全部跑起来，并能完成"注册 → 建库 → 传文档 → 问答"的完整闭环。  
> 适用代码版本：当前仓库（已含 Celery / Redis / JWT 认证 / Alembic / 前端登录态的完整 v2.0 结构）。

---

## 0. 环境前置（先确认装好）

| 依赖             | 版本要求                    | 说明                               |
| -------------- | ----------------------- | -------------------------------- |
| Docker Desktop | 最新                      | 用来起 PostgreSQL(pgvector) 与 Redis |
| Python         | 3.11+（推荐 3.11 / 3.12）   | llama-index 0.12 需要较新版本          |
| Node.js        | 18+（你有 22/24，OK）        | 前端 Vite                          |
| LLM API Key    | OpenAI / 兼容 / 本地 Ollama | 问答与向量化必需；否则接口会报错                 |

---

## 1. 启动基础设施：PostgreSQL + Redis

**PostgreSQL（含 pgvector）** —— 已有 `docker-compose.yml`，直接起：

```bash
cd D:\my_project\web_rag
docker-compose up -d
```

该 compose 会自动：拉起 `pgvector/pgvector:pg16`、建库 `web_rag`、挂载 `init.sql` 启用 `vector` 扩展。

**Redis（⚠️ compose 里没有，必须单独起）**：

```bash
docker run -d --name web_rag_redis -p 6379:6379 redis:7
```

> 为什么必须：后端 `config.py`、限流、Celery broker 全部依赖 Redis；`/api/ready` 也会探 Redis，没它就不是 `ready`。

验证：

```bash
docker ps            # 应看到 web_rag_postgres 与 web_rag_redis 均为 Up
docker exec -it web_rag_redis redis-cli ping   # 返回 PONG
```

---

## 2. 后端配置（.env）

```bash
cd backend
copy .env.example .env      # Windows；Linux/Mac 用 cp .env.example .env
```

编辑 `.env`，**必改项**：

```ini
# 1) LLM —— 不填则问答直接报错
LLM_API_KEY=sk-你的key
LLM_MODEL_NAME=gpt-3.5-turbo        # 或 gpt-4o-mini 等
LLM_BASE_URL=https://api.openai.com/v1

# 2) Embedding —— 维度必须与模型一致！
EMBEDDING_API_KEY=sk-你的key
EMBEDDING_MODEL_NAME=text-embedding-ada-002   # 维度 1536
EMBEDDING_DIM=1536
# 若改用 BAAI/bge-large-zh-v1.5，则 EMBEDDING_DIM 必须改成 1024，否则入库维度错配

# 3) 本地模型方案（可选，免 API Key）
# LLM_PROVIDER=ollama
# LLM_BASE_URL=http://localhost:11434
# EMBEDDING_PROVIDER=huggingface
# EMBEDDING_MODEL_NAME=BAAI/bge-large-zh-v1.5
```

`DB_*`、`REDIS_URL`、`CELERY_*` 默认即可（与上面容器端口一致）。

---

## 3. 安装后端依赖

```bash
cd backend
python -m venv venv
venv\Scripts\activate           # Windows；Mac/Linux 用 source venv/bin/activate
pip install -r requirements.txt
```

> llama-index 体积较大，安装需几分钟。若超时/中断，重跑一次 `pip install -r requirements.txt` 即可。

---

## 4. 建表

**最简单（推荐先用这个）**：直接启动后端，启动时 `main.py` 的 lifespan 会调用 `init_db()` → `Base.metadata.create_all` 自动建好所有表（users / knowledge_bases / documents / chat 等）。

**生产规范做法**：用 Alembic 迁移

```bash
alembic upgrade head
```

验证表已存在（任选）：Docker 里连 PG 执行 `\dt`，应看到多张表；或访问 `/api/ready` 看到 `database: ok`。

---

## 5. 启动后端 API

```bash
cd backend
venv\Scripts\activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

验证（浏览器或 curl）：

- `GET http://localhost:8000/api/health` → `{"status":"healthy"}`
- `GET http://localhost:8000/api/ready` → `{"status":"ready","checks":{"database":"ok","redis":"ok"}}`
- 打开 `http://localhost:8000/docs` 看 Swagger 文档

---

## 6. 启动 Celery Worker（⚠️ 关键，不启动文档永远卡 processing）

新开一个终端：

```bash
cd backend
venv\Scripts\activate
celery -A app.workers.celery_app.celery_app worker --loglevel=info
```

> 文档上传后是异步任务，由 Celery worker 消费执行解析 + 向量化。没起 worker，文档状态会一直停在 `processing`，问答也检索不到内容。  
> 注意：自带的 `start.bat` 没有启动 worker，别完全依赖它。

验证：worker 日志出现 `ready.` 并能连上 Redis broker（无 ConnectionError）。

---

## 7. 启动前端

```bash
cd frontend
npm install
npm run dev
```

浏览器打开 `http://localhost:3000`。Vite 已配置 `/api` 代理到 `localhost:8000`，无需额外跨域配置。

---

## 8. 完整验证闭环

1. 打开 `http://localhost:3000/register` 注册一个账号
2. 跳到 `/login` 登录
3. 进入 `/manage`，创建一个知识库
4. 上传一个 PDF / Markdown 文件
5. **观察 Celery worker 终端日志**，文档状态会从 `processing` → `ready`
6. 进入 `/kb/:kbId/chat`，提问，应看到流式回答 + 引用来源（sources）

全部跑通即代表链路 OK。

---

## ⚠️ 三个会让你卡住的坑（必读）

### 坑 1：Redis 没起

compose 只配了 PostgreSQL。不单独起 Redis，`/api/ready` 的 redis 检查失败，限流、Celery 全部不可用。**务必执行第 1 步的 `docker run ... redis`**。

### 坑 2：Celery worker 没起

文档上传后由 worker 异步处理，不起 worker 文档永远 `processing`。`start.bat` 也没起 worker，需手动按第 6 步起。

### 坑 3：前端 `api.ts` 没注入 token（代码缺陷，会导致登录后 401）

`src/services/api.ts` 里所有 `fetch` 调用**都没有带 `Authorization` 头**，而后端受保护路由要求认证。结果是：能注册、能登录，但之后任何接口（建库/上传/问答）都会返回 `401`。

**最小修复**：在 `api.ts` 顶部加一个带 token 的请求头函数，并合并进每个 fetch 的 `headers`：

```ts
// src/services/api.ts 顶部新增
function authHeaders(): Record<string, string> {
  const token = localStorage.getItem('access_token');
  return token ? { Authorization: `Bearer ${token}` } : {};
}

// 例如 createKnowledgeBase 改为：
export async function createKnowledgeBase(name: string, description: string = '') {
  const res = await fetch(`${API_BASE}/knowledge-bases`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...authHeaders() },
    body: JSON.stringify({ name, description }),
  });
  if (!res.ok) throw new Error('Failed to create knowledge base');
  return res.json();
}
```

> 把每个 `fetch` 的 `headers` 都加上 `...authHeaders()` 即可（涉及 knowledge-bases / documents / chat 等全部接口）。  
> 需要的话我可以直接帮你改好 `api.ts`。

---

## 其他已知小问题

- `frontend/index.html` 引用了 `/vite.svg`，若 `public/` 下没有该文件，浏览器会 404（**无害**，仅缺图标）。可删掉该行或放一个 svg。
- `start.bat` 仅覆盖 PG + 后端 + 前端，**不含 Redis 与 Celery worker**。建议按本指南手动起，或让我帮你把 Redis/worker 也写进启动脚本。
- embedding 维度（`EMBEDDING_DIM`）与所选模型必须严格一致，换模型忘改维度会触发入库报错。

---

## 快速命令清单（复制即用）

```bash
# 1. 基础设施
docker-compose up -d
docker run -d --name web_rag_redis -p 6379:6379 redis:7

# 2. 后端
cd backend
copy .env.example .env        # 记得编辑填 Key
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000

# 3. Celery worker（新终端）
cd backend && venv\Scripts\activate
celery -A app.workers.celery_app.celery_app worker --loglevel=info

# 4. 前端（新终端）
cd frontend
npm install
npm run dev
```

